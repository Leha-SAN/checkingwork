import 'package:flutter/material.dart';

class CodeInputScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ввод кода работ'),
      ),
      body: Center(
        child: Text('Здесь будет экран для ввода кода работ'),
      ),
    );
  }
}